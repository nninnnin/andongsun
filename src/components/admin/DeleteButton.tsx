import React from "react";

const DeleteButton = () => {
  return (
    <div className="btn selector cursor-pointer">
      삭제
      <object data="/remove.svg" />
    </div>
  );
};

export default DeleteButton;
